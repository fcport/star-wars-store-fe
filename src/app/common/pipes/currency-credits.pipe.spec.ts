import { CurrencyCreditsPipe } from './currency-credits.pipe';

describe('CurrencyCreditsPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyCreditsPipe();
    expect(pipe).toBeTruthy();
  });
});
