import { StockStatusAppearancePipe } from './stock-status-appearance.pipe';

describe('StockStatusAppearancePipe', () => {
  it('create an instance', () => {
    const pipe = new StockStatusAppearancePipe();
    expect(pipe).toBeTruthy();
  });
});
